<div class="container">

  <div class="alert alert-secondary text-center">現在登録されているログの一覧です</div>
  <%= link_to "サーバーの故障履歴を確認する", new_trouble_path, class:"btn text-secondary bg-light m-2 d-block" %>
  <%= link_to "応答時間の長いサーバーを調べる", select_servers_path, class:"btn text-secondary bg-light m-2 d-block" %>
  <%= link_to "サーバーの一覧に戻る", servers_path, class:"btn text-secondary bg-light m-2 d-block" %>

  <% @logs.each do |log| %>
    <div>
      <%= log.server.ip_address %>,
      <%= log.checked_at.strftime("%Y%m%d%H%M%S") %>,
      <%= log.interval %>
    </div>
  <% end %>

  </div>